package com.org.careerbuilder.models.enums;

/**
 * Teacher workflow state for an assignment (visibility + lifecycle).
 */
public enum AssignmentPublishStatus {
    DRAFT,
    PUBLISHED,
    CLOSED
}
