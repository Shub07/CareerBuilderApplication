package com.org.careerbuilder.models.enums;

/**
 * Syllabus topic progress (UI: Completed / In progress / Pending).
 * "Covered" in some mocks maps to {@link #COMPLETED}.
 */
public enum LessonPlanTopicStatus {
    COMPLETED,
    IN_PROGRESS,
    PENDING
}
